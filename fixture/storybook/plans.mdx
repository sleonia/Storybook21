# Our plans
- Handle external `Provider`. You can't use <a href="https://mui.com" target="_blank">@miu</a> or other design-system with custom providers now 😔
- Mobile version is't cool. `PropsTable` does't support adaptive 100%
- More customization for config. Favicon, text for i18next
- Some components for playground (CollapsedMenu, ...etc)
- 100% test coverage
- Waiting `@sbol/eslint-config` open source release 😴
