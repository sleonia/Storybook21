export const DIST = 'dist'
export const ENTRY = '/src/index.tsx'
export const ALIAS_PREFIX = '@storybook21-aliases'

export const ALIASES = {
    library: `${ALIAS_PREFIX}/library`,
    playground: `${ALIAS_PREFIX}/playground`,
    libraryTheme: `${ALIAS_PREFIX}/libraryTheme`
}
